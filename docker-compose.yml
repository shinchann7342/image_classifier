services:
  ui_backend:
    build:
      context: ./ui_backend
    ports:
      - "5000:5000"
    environment:
      AI_BACKEND_URL: "http://ai_backend:5001/predict"
    depends_on:
      - ai_backend

  ai_backend:
    build:
      context: ./ai_backend
    ports:
      - "5001:5001"
